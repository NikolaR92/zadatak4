/**
 * Config Module
 * @module src/config
 */

/** Number of rows on a board */
const numberOfRows = 4;

/** Number of coloms on a board */
const numberOfColoms = 4;

const config = {
	numberOfRows,
	numberOfColoms,
};

export default config;
